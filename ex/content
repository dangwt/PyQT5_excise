ex2: < Icon, Button >: a simple applacation with Icon(1.png), use a button to quit.
ex4: < MessageBox >: use a messagebox to ensure if you want to quit.
ex5: < DesktopWidget >: expand ex4, use DesktopWidget to move the window to the center of your screen.
ex6: < QAction, QTextEdit menuBar, toolBar, qApp >: creat a textEdit in window, add a menu and toolbar which tigger off a exit action.
